# Python version requirement
python>=3.8

# Core numerical computation and data processing
numpy>=1.21.0
pandas>=1.3.0

# Data visualization and plotting
matplotlib>=3.5.0

# GUI interface
tkinter>=8.6  # Usually included in the standard library, but installation needs to be ensured
Pillow>=9.0.0  # Image processing (may be a dependency for matplotlib)

# Excel file reading and writing
openpyxl>=3.0.0
xlrd>=2.0.0  # For reading legacy Excel files

# Progress bar display
tqdm>=4.62.0

# Configuration file handling (using standard library json)

# Path operations (using standard library pathlib)

# Mathematical computation (using standard libraries math, random)